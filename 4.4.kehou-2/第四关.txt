1.该可执行文件代码段的第1条指令是什么？(mov0x8(%esp),%eax)
2.该指令第一次被读入内存时，当前进程（用户）代码段的起始地址是多少？(0x10000000)该指令被放在哪个页帧（起始地址）？(0x00fdc000)请通过反汇编显示该页帧中的这条指令。
3.请用bochsdbg跟踪到上述（用户）代码段的起始地址，并进而跟踪到main函数中的fork系统调用的陷入指令，此时该陷入指令的逻辑地址是什么？(0xf:0x8d)线性地址是什么？(0x1000008d)对应的页表项是什么？(0x00fdc027)物理地址是什么？(0x00fdc08d)
